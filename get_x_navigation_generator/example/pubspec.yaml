name: example
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.18.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  get_x_navigation_generator_annotations: ^0.0.5
  # get_x_navigation_generator_annotations: # for local development
    # path: ../../get_x_navigation_generator_annotations
  get: ^4.6.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: ^2.3.3
  get_x_navigation_generator:
    path: ../

flutter:
  uses-material-design: true
